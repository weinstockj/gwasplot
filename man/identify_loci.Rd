% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate.R
\name{identify_loci}
\alias{identify_loci}
\alias{identify_loci.data.frame}
\alias{identify_loci.tbl_df}
\alias{identify_loci.GWASFormatter}
\title{Identify independent GWAS loci using a greedy, distance-based algorithm}
\usage{
identify_loci(x, window_kb = 500, pvalue_threshold = 5e-08, ...)

\method{identify_loci}{data.frame}(x, window_kb = 500, pvalue_threshold = 5e-08, ...)

\method{identify_loci}{tbl_df}(x, window_kb = 500, pvalue_threshold = 5e-08, ...)

\method{identify_loci}{GWASFormatter}(x, window_kb = 500, pvalue_threshold = 5e-08, ...)
}
\arguments{
\item{x}{A GWASFormatter object, data.frame, or tibble.}

\item{window_kb}{Window size in kilobases. Variants within this distance of
the lead variant on the same chromosome are assigned to the same locus.
Default is 500.}

\item{pvalue_threshold}{P-value significance threshold. Default is 5e-8.}

\item{...}{Additional arguments (unused).}
}
\value{
A tibble/data.frame of significant variants with added columns:
  \itemize{
    \item `locus_id` - Integer locus identifier
    \item `is_lead` - Logical, TRUE for the lead (most significant) variant per locus
  }
}
\description{
Groups significant variants into independent loci. The lead variant (most
significant) for each locus is identified first; all other significant
variants on the same chromosome within `window_kb` kilobases are assigned
to that locus. No LD information is used.
}
\section{Methods (by class)}{
\itemize{
\item \code{identify_loci(data.frame)}: Method for data.frame/tibble objects

\item \code{identify_loci(tbl_df)}: Alias for data.frame method

\item \code{identify_loci(GWASFormatter)}: Method for GWASFormatter objects

}}
